/* Tipos primitivos:
Strings, Numbers, Boolean, Null, undefined, symbol
*/

//Strings
 const nome = 'Bruno Bocardi Ferreira';
 const nome2 = "Lucas Bocardi Ferreira";
 const nome3 = `Eduardo Carrijo Ferreira`; // Usando o crase podems fazer templates strings

 // Numbers
 const Nota1 = 10;
 const nota2 = 5;
 const nota3 = 2.54;

 // Boolean 
  const verdadeiro = true;
    const falso = false;
    console.log(falso);
  console.log(verdadeiro);
  

  // undefined
   let apple; // Tipos undefined  ou indefinidos não podem ter valores de variaves const pq ela precisa necessariamente atribuir um valor
   // undefined não tem como starmos otipo quem faz isso é o motor do js 
   console.log(apple);

   // null
     const alunoAprovado = null;
     console.log(alunoAprovado);
